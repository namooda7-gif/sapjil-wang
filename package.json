{
  "name": "sapjil-wang",
  "version": "0.1.0",
  "description": "삽질왕 (Just Dig It) - 캐주얼 아이들 클리커 게임",
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "npm run build && cap sync",
    "cap:android": "npm run build && cap sync android && cap open android",
    "cap:ios": "npm run build && cap sync ios && cap open ios"
  },
  "keywords": [
    "phaser",
    "game",
    "clicker",
    "idle",
    "capacitor",
    "android"
  ],
  "author": "namooda7@gmail.com",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "phaser": "^3.80.1",
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/preferences": "^6.0.0",
    "firebase": "^10.12.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "vite": "^5.2.0"
  }
}
