# 🚀 OnRoom 프로젝트 미션 컨트롤

## 📌 현재 상태 및 목표
- **프로젝트명**: 삽질왕 (Just Dig It)
- **목표**: 5개월 내 글로벌 시장을 타겟으로 한 캐주얼 방치형 클리커 게임 출시
### [2026-05-10] 에너지 드링크 자산 생성 및 스플릿 완료
- **작업 내용**: 제미나이 지침서(`docs/제미나이_드링크_생성_지침서.md`)에 따라 에너지 드링크 4종 시트(1024x1024) 생성, 사장님 투명화 작업 후 4장의 512x512 개별 PNG로 스플릿(분할) 완료.
- **해결된 항목**:
  - `drink_sapcas.png` (삽카스)
  - `drink_hotsaps.png` (핫삽스)
  - `drink_redsap.png` (레드삽)
  - `drink_energasap.png` (에너자삽)
- **저장 위치**: `public/assets/drinks/` 폴더 내에 저장 완료.
- **다음 단계**: Claude에게 "받았어"라고 알리고, BootScene 및 GameScene 코드에 드링크 에셋 교체 자동 적용 진행.

### [2026-05-10] 보물 자산 슬라이싱 및 매핑 오류 수동 교정 (Layer 001 완료 / Layer 002 진행 중)
- **작업 내용**: 기존 자동 슬라이싱에서 2개의 아이템이 1개의 파일로 묶이거나 번호가 밀린 현상을 사장님 지시에 따라 직접 교정.
- **해결된 항목 (Layer 001 - 완료)**:
  - `t_001_01.png` (위:망치 / 아래:황금못) **가로 분할(Top/Bottom)** ➔ 망치(`01`), 황금못(`07`)
  - `t_001_02.png` (좌:페트병뚜껑 / 우:500원) **세로 분할(Left/Right)** ➔ 페트병뚜껑(`02`), 500원(`03`)
  - 기존 `03`➔`04`, `04`➔`05`, `05`➔`06`으로 순차 이동
  - 기존 `06`번 (금덩어리) ➔ `08`번으로 이동
  - 기존 `07`번 (캔커피 빈캔) ➔ `09`번으로 이동
  - `t_001_08.png` (좌:장갑 / 우:빵) **세로 분할(Left/Right)** ➔ 장갑(`10`), 빵(`11`)
  - 기존 `09`번 (위:줄자 / 아래:봉투) **가로 분할(Top/Bottom)** ➔ 줄자(`12`), 봉투(`18`)
  - 기존 `11`번 ➔ `14`번으로 이동
  - 누락된 `13`번(안전점검표), `15`번(스포츠 신문) ➔ 고품질 에셋으로 새로 생성 후 투명화 적용하여 교체 완료
  - `18`번(환갑잔치 봉투) ➔ 크기가 너무 작던 문제를 512x512 캔버스에 꽉 차게 리사이징하여 해결
  - `01`, `02`, `03`, `07`, `12`번 ➔ 이미지 크기가 다소 작아 **30% 확대(Scale Up)** 적용하여 가시성 개선
- **해결된 항목 (Layer 002 - 완료)**:
  - 기존 `06`➔`07`, `09`➔`06`, `07`➔`08`로 순차 및 교차 이동
  - `t_002_08.png` (좌:연필 / 우:빵) **세로 분할(Left/Right)** ➔ 연필(`09`), 빵(`10`)
  - 기존 `10`➔`13`, `13`➔`12`로 순차 이동
  - `t_002_12.png` (좌:야구카드 / 우:컴퍼스) **세로 분할(Left/Right)** ➔ 야구카드(`15`), 컴퍼스(`11`)
  - 누락/오류인 `14`번(찢은 시 노트) ➔ 고품질 에셋으로 새로 생성 후 투명화 적용하여 교체 완료
  - `09`, `10`, `11`, `15`번 ➔ 이미지 크기가 작아 **30% 확대(Scale Up)** 적용하여 가시성 개선

### [2026-05-10] 보물 자산 슬라이싱 및 매핑 오류 수동 교정 (Layer 004 완료 / Layer 005 진행 중)
- **작업 내용**: 사장님 지시에 따라 Layer 004, 005 파일 분할 및 밀어내기 교정.
- **해결된 항목 (Layer 004 - 완료)**:
  - `t_004_07.png` (위:비밀 금고열쇠 / 아래:빗) **가로 분할(Top/Bottom)** ➔ 비밀 금고열쇠(`07`), 빗(`12`)
  - 기존 `12`➔`14`번으로 이동
  - `t_004_08.png` (위:루이뷔돌 가방 / 아래:빨대) **가로 분할(Top/Bottom)** ➔ 루이뷔돌 가방(`08`), 씹힌 식혜 빨대(`13`)
  - 기존 `13`➔`15`번으로 이동
  - 기존 `15`➔`16`번으로 이동
  - 기존 `16`번(위:슬리퍼 중복 / 아래:폴더폰) **가로 분할(Top/Bottom)** ➔ 폴더폰(`17`)
  - 기존 `17`번(위:폴더폰 중복 / 아래:적자 일기) **가로 분할(Top/Bottom)** ➔ 적자 일기(`18`)
  - 기존 `18`➔`19`번으로 이동
  - `07`, `08`, `11`, `12`, `13`, `14`, `17`, `18`번 ➔ 1차 **30% 확대(Scale Up)** 적용하여 가시성 개선
  - 기존 `14`번(양머리 수건) ➔ 단순 양 머리 형상으로 잘못 생성된 오류를 확인하고, **사우나 양머리 수건 형태의 고품질 에셋으로 새로 생성**하여 투명화 후 교체 완료
  - `17`번(폴더폰) ➔ 크기가 여전히 작아 **추가 30% 확대** 적용

- **해결된 항목 (Layer 005 - 진행 중)**:
  - 기존 `10`➔`12`번으로 이동, 기존 `09`➔`11`번으로 이동
  - `t_005_06.png` **가로 분할(Top/Bottom)** ➔ 금괴(`08`), 행정병의 주사위(`13`)
  - 기존 `03`, `04`, `05` ➔ 각각 `05`, `06`, `07`번으로 이동
  - `t_005_01.png` **세로 분할(Left/Right)** ➔ 좌측(`01`), 우측(`02`)
  - `01`번(군용 수통), `02`번(군번줄), `04`번(전투화) ➔ 원본 3x3 시트(첫 번째)의 맨 윗줄 3개 이미지를 각각 스플릿 및 흰색 배경 투명화 처리하여 교체 완료
  - `09`번(시계), `10`번(살렌타인) ➔ 원본 3x3 시트가 크로마키(녹색)가 아닌 **흰색 배경**이었던 점을 파악하여, 스크립트를 수정해 흰색 배경을 완벽하게 투명화 처리 후 재추출 완료
  - `03`, `08`, `13`번 ➔ 이미지 크기가 작아 **1차 30% 확대(Scale Up)** 적용하여 가시성 개선
  - `13`번, `03`번 ➔ 여전히 작게 보여 **추가 30% 확대** 적용
  - `14`번(마른 건빵) ➔ 고품질 에셋으로 새로 생성한 뒤 투명화 처리하고, 주변 아이템 대비 커보여서 **30% 축소(Scale Down)** 하여 비율 맞춤 완료
  - `15`번(부모 통화 메모지) ➔ 이전 레이어 작업에 썼던 '깨끗한 노란색 메모지' 자산을 재활용해, 맑은 고딕 폰트로 **"엄마. 돈좀 보내줘."** 텍스트를 직접 합성하여 교체 완료
- **결과**: 지시하신 Layer 005 파일들이 메모리 상에서 안전하게 연쇄 이동, 분할, 재추출, 텍스트 합성, 2차 리사이징까지 처리되어 **전체 완벽 종료**됨.
- **다음 단계**: 사장님의 `treasure_check.html` 화면 검수 후, 마지막 Layer 006(콘서트장) 지시 대기.

### [2026-05-10] 보물 자산 114종 슬라이싱, 매핑 및 미세조정(리사이징/텍스트 합성) 100% 완료
- **작업 내용**: 자동 슬라이싱에서 발생한 묶임, 밀림, 위치 오류를 1번부터 6번 레이어까지 전수 검사하고 직접 스플릿(분할) 및 좌표/인덱스 교정.
- **해결된 항목 (Layer 006 - 콘서트장 완료)**:
  - 기존 `10`~`17`번 ➔ `11`~`18`번으로 연쇄 밀어내기(Shift) 완료
  - `01`~`06`번(상단 2열) ➔ 원본 3x3 시트(첫 번째)에서 6개 이미지를 순서대로 스플릿하여 흰색 배경 투명화 처리 후 교체 완료
  - `01`~`06`번 ➔ 주변 아이템과의 비율을 맞추기 위해 일괄 **20% 축소(Scale Down)** 적용
  - `07`번(레전드 데뷔쇼 티켓) ➔ 사장님이 직접 배경 누끼를 딴 VIP 티켓 원본을 덮어씌워 교체 완료
  - `08`, `09`, `10`번(하단 1열) ➔ 원본 3x3 시트 맨 아랫줄(달러, 가방, 짠물)을 스플릿 및 투명화하여 각각 08, 09, 10번에 배치 완료
  - `08`~`10`번 ➔ 일괄 **20% 축소(Scale Down)** 적용하여 비율 조정
  - `11`~`13`번 ➔ 크기가 작아 가시성 개선을 위해 일괄 **30% 확대(Scale Up)** 적용
- **최종 결과**: 레이어 001부터 006까지 총 114개의 보물 자산이 완벽하게 분할, 투명화, 비율 조정(Scale Up/Down) 및 텍스트 합성 처리되어 **100% 완벽하게 구축 완료**되었습니다.
- **다음 단계 (To-Do)**:
  1. `src/data/treasures.json` 파일 생성 및 데이터 연동 확인.
  2. 인게임(`MuseumScene`, `GameScene`)에서 114개 자산들이 정상적으로 출력되는지 엔진 테스트 연동.

### [2026-05-08] 보물 자산 114종 전면 재생성 및 Smart Slicing 완료
- **작업 내용**: 지침서에 따라 Layer 001~006 보물 114종 시트 생성 완료. 
- **문제 해결**: AI가 불규칙하게 배치하여 발생하는 이미지 겹침 현상을 해결하기 위해, 픽셀 단위로 객체를 탐지하는 **Smart Slicing 알고리즘(Connected Component Labeling)**을 독자 개발하여 적용.
- **결과**: `public/assets/treasures/new/` 폴더 내에 정확히 114장의 완벽한 투명 배경(Alpha) 512x512 개별 PNG 파일이 저장됨. (`t_001_01.png` ~ `t_006_19.png`)
- **다음 단계**: `BootScene.js` 이미지 로드 경로 및 `MuseumScene`, `GameScene` 데이터 매핑(JSON) 연동.

### [2026-05-08] 이전 작업 내역 (폐기/수정)
- **성취도**: 보물 자산 개선 작업 100% 완료 (폐기된 내용).
- **차기 작업 예약 (To-Do)**:
  1. **이미지 Slicing 및 투명화**: 생성된 흰색 배경 시트에서 개별 아이콘 추출 및 알파 채널 적용.
  2. **데이터 매핑**: `src/data/treasures.json` 생성 및 연동.
  3. **인게임 통합**: `MuseumScene` 및 `GameScene`에서 새 자산 출력.

### [2026-05-03] 캐릭터 이미지 생성 및 자산 관리
- **지침서 기반 캐릭터 생성**: `docs/제미나이_캐릭터_생성_지침서.md`의 규칙에 따라 게임 캐릭터 이미지 생성 중.
- **생성 완료 항목**:
  - `char_001` (박삽돌): `tired`, `panic`, `dig_hard` (3장 추가 완료)
  - `char_002` (굴착용): 전체 포즈 8장 생성 및 배치 완료
  - `char_003` (김파순): `idle`, `dig`, `combo`, `surprise`, `clear` (5장 생성 및 배치 완료)
- **파일 경로**: `public/assets/characters/` 내에 투명 배경 PNG로 저장됨.
- **성취도**: 총 43장 중 16장(약 37%) 생성 및 자산화 완료.

## 🛠 발견된 이슈 및 개선 필요 항목 (게임 성공을 위한 핵심 과제)
1. **타격감 강화 (햅틱 및 사운드)**:
   - *문제점*: 현재 콤보에 대한 시각적 피드백이나 찰진 사운드/햅틱이 부족함.
   - *목표*: 기획서에서 강조된 가장 중요한 요소로, 1500ms 윈도우 콤보 달성 시 'Combo!' 연출, 흙/돌/모래 등 레이어별 특수 사운드 및 모바일 진동 구현 시급.
2. **성장 루프 부재 (상점 미작동)**:
   - *문제점*: 코인을 획득해도 '상점' 씬이 연결되어 있지 않아 소모할 곳이 없음.
   - *목표*: 무한 반복의 동기부여를 위해 삽 업그레이드 및 캐릭터 해금을 위한 `ShopScene` 구현 필요.
3. **방치형(Idle) 요소 추가**:
   - *문제점*: 직접 탭하지 않으면 재화가 벌리지 않는 순수 클리커 상태.
   - *목표*: 오프라인 보상이나 '자동 삽질 인부' 등을 도입하여 방치형(Idle)의 편의성을 더해야 함.
4. **전시관(뻘짓 랭킹) 및 UI 보완**:
   - *문제점*: 게임 중 메인 메뉴로 나가는 버튼 미작동 및 '삽질박물관' 미구현.
   - *목표*: 인게임 내비게이션 복구, 보물을 모으고 자랑하여 바이럴을 일으킬 핵심 콘텐츠인 전시관/뻘짓 랭킹 개발 착수 필요.

---
*본 문서는 사장님이 한눈에 프로젝트 상태를 파악할 수 있도록 주요 작업 후 최신화됩니다.*
