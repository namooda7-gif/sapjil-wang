# **삽질왕 (Just Dig It) — 2주 리텐션 개선 작업 문서**

**목적**: "탭 한 번 \= 삽질 한 번"의 단순 클리커를, *돌아올 이유가 있는* 게임으로 끌어올린다. **핵심 프레임**: 메타 시스템은 **리텐션**만이 아니라 \*\*"공유 가능한 순간(클립/캡처거리)"\*\*을 만들어야 한다. 이 게임은 *잘 만들어서* 성공하는 게 아니라 *퍼져서* 성공한다. **작성 기준**: 컨설팅 브리프(2026-06-01)의 §5(재론 금지)·§6(핵심 질문)을 준수.

---

## **0\. 절대 규칙 (모든 단계에 공통 적용)**

작업 중 **절대 건드리면 안 되는 것** — 위반 시 대표 결정/별도 트랙과 충돌:

- ❌ `requiredDigs`(필요 탭 수) **하향 금지** — 진행속도는 탭 수가 아니라 1탭 효율(배율)로 조절하기로 확정됨.  
- ❌ 장애물 **개별 난이도(요구탭 6\~12) 변경 금지** — 대표가 4차 푸시에서 직접 2배로 올린 의도된 결정.  
- ❌ 통화 **시각 표시는 금괴(🪙)만**, 💵 달러 표시 금지 — 글로벌=미국중심 인상 회피.  
- ❌ 버그 트랙(시작버튼 입력 / 굴 시작점-지표면 정렬 / 클리어 직전 지상 텍스처 잔존) **손대지 말 것** — 별도 처방 진행 중.  
- ❌ **배율 곱연산 인플레 주의** — 자동삽질 수입은 탭 수입에 곱하지 말고 별도 스트림으로.

---

## **1\. 컨설팅 질문 답변 요약 (브리프 §6)**

| \# | 질문 | 결론 |
| :---- | :---- | :---- |
| Q1 | 리텐션 엔진: 프레스티지 vs 자동삽질? | **자동삽질 1개만** 출시 전 도입. 프레스티지는 출시 후. |
| Q2 | 콘텐츠 절벽 해소? | **무한 심층 모드** (기존 텍스처 wrap, 에셋 0개 추가). |
| Q3 | 코인 싱크? | 자동삽질 무한 레벨업(메인) \+ 삽 가산 강화 \+ 다이아/유물 소비처. |
| Q4 | 인터럽션 밀도? | **난이도 유지, 빈도만↓** (장애물 20탭→40탭 \+ 전역 쿨다운). |
| Q5 | 수집 메타? | 도감 완성 보너스 \+ 일일 미션 \+ 로컬 주간 목표. |
| Q6 | 2주에 손댈 3가지? | ①자동삽질 ②무한 심층 ③일일 미션+도감 (+ 빈도 튜닝 워밍업). |

### **단계 분류**

- **\[출시전-필수\]**: 콘텐츠 절벽 / 메타+싱크(자동삽질) / 수집 메타 / 인터럽트 빈도 튜닝  
- **\[출시후-성장\]**: 소셜·랭킹(Firebase) / 광고·IAP 훅

---

## **2\. 2주 실행 순서 (위험 낮은 순)**

STEP 0 (30분)   인터럽트 빈도 튜닝 — 숫자만 변경, 즉시 체감

STEP 1 (반나절)  무한 심층 모드 — "게임 오버" 벽 제거, 에셋 0

STEP 2 (가장 큼) 자동삽질 시스템 — 2a→2b→2c 쪼개서

STEP 3 (중간)   일일 미션 \+ 도감 완성 보너스

**현실 조언**: STEP 2가 제일 크다. 시간이 모자라면 **STEP 1+2까지만 출시**하고 STEP 3은 출시 후로 미뤄도 된다. 한 번에 다 바꾸지 말 것 — 단계마다 commit하고, 하나 끝내고 테스트한 뒤 다음으로.

---

## **3\. CLI 지침 (단계별 복붙용)**

### **✅ STEP 0 — 인터럽트 빈도 튜닝**

GameScene.js 인터럽트 빈도만 조정해줘.

장애물 개별 난이도(요구탭)는 절대 건드리지 마.

빈도만 낮춰서 삽질 리듬 회복하는 게 목표야.

1\. 장애물 spawnObstacle 발동 주기:

   매 20탭 → 매 40탭으로 변경.

2\. 전역 인터럽트 쿨다운 추가:

   장애물/드링크/날씨/POWER DIG 중

   하나가 발동하면 그 후 12탭 동안

   다른 인터럽트 발동 금지.

   (보물 팝업과 혼잣말은 쿨다운에서 제외)

3\. 드링크/날씨/POWER DIG의 '첫 N탭 보장' 값은

   그대로 두되, 반복 발동 확률만 절반으로.

지키기:

\- requiredDigs 변경 금지

\- 장애물 요구탭(6\~12) 변경 금지

\- 버그 트랙(시작버튼/정렬/텍스처) 손대지 마

한국어 주석 필수.

**테스트**: 레이어 1 한 판 → "장애물 깨기"에 쓰는 탭이 줄고 순수 삽질 리듬이 사는지 확인.

---

### **✅ STEP 1 — 무한 심층 모드**

GameScene.js loadLayer()/clearLayer() 수정해서

무한 심층 모드 추가해줘.

현재: 레이어 6 클리어 시 '한국 정복 완료' 뜨고

메인 메뉴로 강제 복귀 → 여기서 게임이 끝남.

변경:

1\. 레이어 6 클리어 후 메뉴 복귀 대신

   '무한 심층 모드' 진입.

2\. 무한 심층:

   \- 기존 지하 텍스처 무한 wrap (B-2 루프 그대로)

   \- '심층 단계'마다 requiredDigs 상승

     (절대 낮추지 마, 오직 상승만)

   \- 깊이 단계가 깊어질수록 코인 보상 배율 상승

   \- 보물은 기존 114종 풀에서 랜덤 출현,

     깊을수록 rare/epic/legendary 가중치 상승

3\. 깊이 마일스톤(예: 매 100m)마다

   다이아 또는 뻘짓 점수 보너스.

4\. HUD에 현재 심층 단계와 깊이(m) 표시.

지키기:

\- requiredDigs는 상승만, 절대 하향 금지

\- 통화 표시는 금괴, 달러 금지

\- 기존 콤보/삽 배율/사운드/연출 전부 유지

한국어 주석 필수.

**테스트**: 레이어 6 클리어 → 멈추지 않고 계속 더 깊이 파지는지, 보상이 커지는지 확인.

---

### **✅ STEP 2 — 자동삽질(인부) 시스템 ★핵심**

**2a → 2b → 2c 순서로. 각각 테스트하고 넘어갈 것.**

#### **2a. 로직/데이터 (UI 없이)**

자동삽질 시스템의 데이터와 로직만 먼저 만들어줘.

UI는 다음 단계에서 할 거니까 지금은 만들지 마.

새 파일 src/managers/AutoDigManager.js 생성 (localStorage):

컨셉: 박삽돌은 이제 십장. 인부를 고용해 자동으로 판다.

인부 3종 (각각 무한 레벨업):

\- 막내인부:     기본가 200,   초당코인 1

\- 포크레인기사: 기본가 2000,  초당코인 12

\- 굴착기:       기본가 25000, 초당코인 150

레벨업 공식:

\- 비용 \= 기본가 × 1.15^(현재레벨)

\- 초당코인 \= 기본초당 × 현재레벨

핵심 규칙 (인플레 방지):

\- 자동삽질 수입은 '초당 코인' 별도 스트림.

\- 탭 수입(콤보×삽 배율)에 곱하지 마. 절대 곱연산 금지.

\- 총 자동수입 \= 모든 인부의 (초당코인 합).

\- 게임 플레이 중 매초 자동으로 코인 가산.

CurrencyManager와 연동해서 코인 차감/획득 처리.

한국어 주석 필수. UI는 만들지 마.

**테스트**: 로그로 인부 고용→레벨업→초당 코인 증가가 도는지만 확인.

#### **2b. UI 패널**

AutoDigManager용 UI 패널을 GameScene에 추가해줘.

화면 하단 또는 우측에 '인부' 버튼.

누르면 인부 관리 패널 오픈:

\- 인부 3종 목록 (이름, 현재 레벨, 초당 코인, 레벨업 비용)

\- '고용/레벨업' 버튼 (코인 충분하면 활성)

\- 총 초당 자동 코인 표시

비주얼: 통화는 금괴 아이콘, 달러 금지.

코믹/공유 포인트 (중요):

\- 각 인부도 번아웃 컨셉. 레벨 높아지면

  말풍선 랜덤 출현:

  막내인부:     '사수님 저 그만둘래요...'

  포크레인기사: '시급 올려주세요...'

  굴착기:       '기름값이 더 나와요...'

\- 이 패널이 캡처해서 공유하고 싶게 만들어.

기존 게임 화면/탭 삽질 동작 방해하지 않게 depth 관리.

한국어 주석 필수.

**테스트**: 패널 열고 고용/레벨업 누르면 코인 줄고 초당 수입 늘어나는지.

#### **2c. 오프라인 보상 연동**

OfflineRewardManager를 자동삽질과 연동해줘.

현재: 초당 0.5코인 고정, 8시간 캡.

변경:

\- 오프라인 수입 \= AutoDigManager 총 초당코인 × 떠나있던 초

\- 캡: 8시간 (인부 없으면 수입 0\)

\- 복귀 시 '인부들이 N코인 벌어놨어요\!' 팝업

이렇게 하면 인부 레벨업이 곧 오프라인 수입 증가 →

돌아올 이유가 생김.

기존 출석(AttendanceManager)과 충돌 없게.

통화 표시 금괴, 달러 금지.

한국어 주석 필수.

**테스트**: 게임 끄고 몇 분 뒤 재접속 → 인부 수입만큼 코인 쌓였는지.

---

### **✅ STEP 3 — 일일 미션 \+ 도감 완성 보너스**

두 가지 추가해줘. 새 파일 src/managers/MissionManager.js (localStorage).

1\. 일일 미션 (자정 리셋):

   매일 랜덤 3개 출제:

   \- 콤보 30 달성 3회

   \- 보물 10개 발견

   \- 장애물 5개 격파

   \- 지하 50m 도달

   \- 자동수입으로 5000코인 획득

   각 완료 시 코인/다이아/유물 보상.

   메인 또는 게임 화면에 미션 진행도 표시.

2\. 도감 완성 보너스:

   MuseumScene에 레이어별 보물 도감 추가.

   \- 레이어별 수집 진행도 표시 (예: 17/20)

   \- 한 레이어 세트 완성 시 영구 보너스

     (예: 그 레이어 보물 확률 \+5%)

   \- 완성 시 '도감 완성\!' 뱃지 \+ 캡처용 연출

지키기:

\- 통화 표시 금괴, 달러 금지

\- 기존 박물관 뻘짓 점수 유지

한국어 주석 필수.

---

## **4\. 2주 체크리스트**

### **1주차**

- [ ] **STEP 0** 인터럽트 빈도 튜닝 → 테스트 → commit  
- [ ] **STEP 1** 무한 심층 모드 → 테스트 → commit  
- [ ] **STEP 2a** AutoDigManager 로직 → 로그 테스트 → commit  
- [ ] **STEP 2b** 인부 UI 패널 → 테스트 → commit

### **2주차**

- [ ] **STEP 2c** 오프라인 보상 연동 → 테스트 → commit  
- [ ] 자동삽질 밸런싱 숫자 조정 (기본가/초당코인/1.15 배율 실측 튜닝)  
- [ ] **STEP 3** 일일 미션 \+ 도감 보너스 → 테스트 → commit  
- [ ] 통합 플레이테스트: 신규 유저 첫 30초 → 첫 보물/첫 인부까지 흐름 점검  
- [ ] (여유 시) 무한 심층 보상 곡선 미세조정

---

## **5\. 안전 수칙 (오늘 같은 사고 방지)**

1\. STEP 시작 전마다:

   git add . && git commit \-m "step N 시작 전 백업"

2\. 한 STEP 안에서도 2a→2b→2c 쪼개서, 하나씩 테스트

3\. 꼬이면:

   git reset \--hard   (마지막 commit으로 복구)

4\. 절대 건드리면 안 되는 것:

   \- requiredDigs 하향 (진행속도 결정 위반)

   \- 장애물 요구탭 (대표 결정)

   \- 시작버튼/정렬/텍스처 버그 (별도 트랙)

   \- 배율 곱연산 (인플레 — 자동수입은 별도 스트림)

---

## **6\. 출시 전략 메모 (분석에서 도출 — 코딩 외)**

이 게임은 *퍼져야* 산다. 마케팅 예산 0원 1인 개발의 유일한 길은 **오가닉 바이럴**.

- **모든 신규 시스템 \= 클립 가능한 순간**으로 설계 (번아웃 연출, 황당 보물, 인부 말풍선, 도감 완성 뱃지).  
- **한국 먼저** — K-유머/번아웃 정서가 가장 잘 먹히는 시장. 글로벌/다국어는 반응 확인 후.  
- **스코프 잔인하게 축소** — 6레이어 \+ 무한 심층 \+ 캐릭터 6\~8 \+ 박물관으로 *먼저 출시*. 72레이어·24캐릭터는 출시 후 라이브옵스.  
- **개발 과정 자체를 숏폼으로 기록** ("비개발자가 AI로 게임 만들기") — 게임보다 *만드는 사람 이야기*가 먼저 퍼질 수 있다.  
- **첫 30초에 승부** — 켜고 30초 안에 "어 웃기네" 하고 캡처 안 하면 그 유저는 안 돌아온다.

---

## **부록: 파일 지도**

| 영역 | 파일 |
| :---- | :---- |
| 핵심 루프 | `src/scenes/GameScene.js` — `dig()` 1030, `loadLayer()` 887, `clearLayer()` 2975, `spawnObstacle()` 4368, `setCharacterState()` 3960 |
| 데이터 | `src/data/layers.js`(6), `characters.js`(30), `shovels.js`(3) |
| 재화/저장 | `src/managers/CurrencyManager.js`, `OfflineRewardManager.js`, `AttendanceManager.js` (전부 localStorage) |
| 신규 생성 | `src/managers/AutoDigManager.js`, `src/managers/MissionManager.js` |
| 화면 | `MenuScene` / `GameScene` / `ShopScene` / `MuseumScene` / `CharacterScene` / `BootScene` (`main.js` 66행 등록) |
| 사운드·햅틱 | `src/managers/SoundManager.js` |
| 아직 없음 | FirebaseManager, RankScene, MapScene, IAP, 광고 (전부 출시 후) |

---

*숫자(인터럽트 빈도, 인부 기본가/초당코인, 1.15 배율, 보상 곡선)는 출발점일 뿐 실측 후 조정 필요. 동의 안 되는 설계는 반박하고 수정할 것.*  

---

## **7\. 오늘 실제 적용 결과 (2026-06-01 — 전 단계 실행 완료)**

**브랜치**: `feature/fun-upgrade-2026-06` (main 보존). 단계마다 백업 커밋 + 빌드/문법 검증 통과. 인게임 검수는 대표 대기(탭 게임이라 자동검증 불가).

### **계획 STEP → 실제 커밋 매핑**

| 계획 | 실제 커밋 | 상태 |
| :---- | :---- | :---- |
| 베이스라인(.gitignore 정리 + 컨설팅 브리프) | `01d1ee0` | ✅ |
| STEP 0 인터럽트 빈도 | `7093c8e` | ✅ |
| STEP 1 무한 심층 모드 | `eba0cd1` | ✅ |
| STEP 2a 자동삽질 로직 | `f0502f3` | ✅ |
| STEP 2b 인부 UI 패널 | `a4d7584` | ✅ |
| STEP 2c 오프라인 보상 연동 | `36ee61c` | ✅ |
| STEP 3 일일 미션 + 도감 보너스 | `b2e7f57` | ✅ |
| (추가) 핫패스 성능 보강 | `12dee1c` | ✅ |
| (추가) 녹음 삽질 효과음 매핑 | `4ed10df` | ✅ |

### **계획에 없던/빠졌던 항목 (이번 검토로 보강)**

1. **녹음 삽질 효과음(SFX) 매핑** — *원 문서에 없던 작업.* `public/assets/sounds/sfx`의 녹음 14종을 로드, 탭마다 **2~3개를 detune(피치)/볼륨/딜레이 변주로 겹쳐 재생** → 두툼한 타격감 + 단조로움 방지.
   - soilType 매핑: dirt/sand→흙(soil), tile/concrete→콘크리트+돌, 무한심층→콘크리트. 장애물 rock/bone/skull/root→녹음 믹스, 그 외(얼음·철판 등)→기존 합성.
   - 파일 누락 시 기존 합성음으로 자동 폴백(무음/에러 없음). 공백 포함 파일명(`dig_obstacle__root2 .mp3`) 정리.
   - 메모: 레이어 2(모래)는 전용 모래 녹음이 없어 흙으로 대체. `rock` soilType은 현재 어떤 레이어도 안 써서 무한심층/타일 믹스에만 섞임. 필요 시 `dig_sand_*` 추가 요청.
2. **핫패스 성능 보강** (저사양 안드로이드 입력 지연 방지) — 미션 `auto_coin` 보고를 매 프레임→1초 배치, `depth`는 정수 m 증가 시에만 보고, 도감 완성 수는 캐시(매 탭 114종 스캔 제거).
3. **무한심층 구현 디테일** — 심층 레이어는 layer_006 자산 재사용(`bgId`). `ensureLoopTexture`에 `currentBgSrcId` 전달 → `deep_N_bg` 부재로 인한 지하 무한루프 실패(= 별도 버그 트랙의 "지상 텍스처 잔존" 재발)를 사전 차단.
4. **인플레 결정 확정** — 무한심층 코인배율은 **탭+보물 수입에만** 적용, **자동삽질 수입엔 미적용**(이중 곱연산 방지).

### **인터럽트 빈도 실제 적용값**

장애물 20→40탭, 전역 인터럽트 쿨다운 12탭(보물·혼잣말 제외), 드링크 0.12→0.06 / 날씨 0.08→0.04 / POWER DIG 0.25→0.125 (첫 N탭 보장값은 유지, 쿨다운 시 보류 후 발동).

### **신규/수정 파일**

- 신규: `src/managers/AutoDigManager.js`, `src/managers/MissionManager.js`
- 수정: `GameScene.js`, `MenuScene.js`, `MuseumScene.js`, `CurrencyManager.js`, `OfflineRewardManager.js`, `BootScene.js`, `SoundManager.js`, `data/layers.js`
- 자산: `public/assets/sounds/sfx/` 14종

### **남은 튜닝 (인게임 실측 후)**

- 자동삽질 기본가/초당코인/1.15 배율, 무한심층 코인배율·보물 가중치 곡선
- SFX 볼륨(`DIG_SFX_VOLUME` 0.9)·믹스 개수(2~3)·피치 변주폭(±220 cents)
- 우상단 🎁/📋 버튼 약 6px 겹침(미관), 무한심층 100m 마일스톤은 ~1000탭이라 기본 플레이서 드묾(정상)
